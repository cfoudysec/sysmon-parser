theme: jekyll-theme-hacker
title: sysmon-parser
description: A small Python CLI that parses Sysmon Event ID 1 (Process Creation) XML logs into JSON / JSONL / CSV, with first-class handling of base64-encoded PowerShell payloads.
